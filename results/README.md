# Results directory

Analysis scripts create outputs here. Only release_tables/ is versioned because it contains compact final tables supporting the central results. Other generated files are ignored and can be recreated from the public inputs.
